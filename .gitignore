.symbui/
.backups/
.DS_Store
node_modules/

# Local demo artifacts written by tests/review-loop-demo.mjs (under the session
# root above).

# Vite leaves these behind when a config file fails to load.
vite.config.*.timestamp-*.mjs
.vite/
annotate-web-ui/scripts/native/.build/
